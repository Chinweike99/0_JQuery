$(document).ready(function() {
    $("h1").css("color", "red"); // $(document).ready(function(): This checks if the jQuery is full ready, then its executes
});                              // it enables jQuery to excute despite the position in html file
$("button").text("click me"); // This changes all the text in  the button
 